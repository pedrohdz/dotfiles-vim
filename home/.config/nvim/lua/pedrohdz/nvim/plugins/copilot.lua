return {
  'zbirenbaum/copilot.lua',
  requires = {
    'copilotlsp-nvim/copilot-lsp', -- (optional) for NES functionality
  },

  config = true,

  -- -- Lazy loading
  -- lazy = true,
  -- cmd = 'Copilot',
  -- event = 'InsertEnter',
}
